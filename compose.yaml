# =============================================================================
# ATL Chat - Root Docker Compose
# =============================================================================
# Orchestrates all services across the entire stack
# Usage:
#   docker compose up -d              # Start all services
#   docker compose --profile dev up   # Start with dev services
# =============================================================================

name: atl-chat

# Include shared infrastructure
include:
  - infra/docker/compose/networks.yaml
  - infra/docker/compose/volumes.yaml
  - apps/irc/compose.yaml
  - apps/xmpp/compose.yaml

# Root-level services (monitoring, logging, etc.)
services:
  # Placeholder for future root-level services
  # Examples: Prometheus, Grafana, Traefik, etc.
  placeholder:
    image: alpine:latest
    command: echo "Root compose loaded successfully"
    profiles:
      - placeholder
